# 방탈출N 프로젝트 메모

## 공유 데이터 스토어 (room-store.js)
`room-store.js` 는 모든 방탈출 데이터의 단일 원천이다. localStorage(`bangtalN.rooms.v3`)에 저장되며, 최초 1회 SEED(r1~r6)로 초기화된 뒤 편집/생성/삭제가 유지된다. 하드코딩 금지 — 화면은 이 스토어에서 읽고 쓴다.
- 각 화면 `<helmet>`에 `_ds_bundle.js` 다음 줄로 `<script src="room-store.js"></script>`(모바일 폴더는 `../room-store.js`)를 로드한다.
- `window.RoomStore` API: `all()`(목록), `get(id)`, `toPlay(id)`(authoring pages→플레이 시나리오 변환), `save(room)`, `saveMeta(id,patch)`, `remove(id)`, `duplicate(id)`, `create(meta)`, `resetAll()`, `THUMBS`.
- 방탈출 모델: `{ id, title, thumb, pin, vis(public|private), date, order, hintPolicy(free|count|deduct), hintCount, pages[], play? }`. **`status` 필드는 제거됨** — 아래 "열림 상태" 절 참고. `pages[]`는 편집화면 authoring 모델(노드 x/y·lock·choices·correctExit 등). r1은 자물쇠 5종 리치 `play`를 별도 보유, 나머지는 pages에서 자동 파생.
- 연동 현황: 방탈출 편집화면(=`?id=`로 로드, 변경 시 자동 저장·뒤로가기 시 저장 후 My 방탈출로), My 방탈출(스토어 목록·생성/복제/삭제·카드 링크에 `?id=`), 참여자 플레이(웹·모바일, `?id=`로 로드), 리포트 상세/개인별(헤더 제목·PIN·문제수만 스토어 room에서, 분석 통계는 백엔드 없어 합성 유지), 문제 대화 편집(`?id=&page=`로 페이지 로드, 대화 blocks+문제를 `page.dialog`에 저장하고 body·lock·정답 파생, 저장 시 편집화면으로 복귀).

## 스토어 싱글턴 + 버전 가드 (room-store.js / session-store.js) ★
두 스토어는 **버전 있는 싱글턴**이다. 파일 첫 줄이 이렇게 생겼다.
```js
var VERSION = 3;   // API 를 추가/변경하면 이 숫자를 올릴 것
if (window.SessionStore && (window.SessionStore.__v || 1) >= VERSION) return;
```
- **싱글턴이 필요한 이유:** 화면 전환 등으로 `<helmet>` 스크립트가 두 번 평가되면 인스턴스가 두 개 생기고 각자 별도 메모리 배열(`rooms`/`sessions`)을 갖는다. 그러면 **한쪽에 쓴 변경이 localStorage 엔 반영되지만 화면이 읽는 쪽 메모리엔 안 보여** UI 가 멈춘다.
- **버전 비교가 필요한 이유:** 단순히 `if (window.SessionStore) return;` 이면, **구버전 인스턴스가 먼저 올라와 있을 때 새 API 가 영원히 로드되지 않는다.** → `store.roundsForRoom is not a function` 같은 런타임 오류. 실제로 발생했던 버그다(회차 목록 화면).
- **⚠️ 스토어에 함수를 추가하거나 시그니처를 바꾸면 반드시 `VERSION` 을 올릴 것.** 안 올리면 이미 열려 있던 탭/화면에서 새 API 를 못 본다.
- 내보내는 객체에 `__v: VERSION` 을 넣어야 가드가 판단할 수 있다.

## 실시간 세션 스토어 (session-store.js)
`session-store.js` 는 실시간 모드의 단일 원천이다. `room-store.js`가 "콘텐츠(방)"를, 이 파일이 "1회 실행(세션)"을 담당한다. localStorage(`bangtalN.sessions.v1`) + `storage` 이벤트로 **같은 브라우저의 다른 탭끼리 실제 동기화**된다(백엔드 없음 — 진행자 탭에서 조작하면 참여자 탭이 즉시 반응).
- 실시간 화면 `<helmet>`에 `room-store.js` 다음 줄로 `<script src="session-store.js"></script>`(모바일은 `../session-store.js`).
- `window.SessionStore` API: `subscribe(fn)`(변경 구독—화면 리렌더), `create(roomId,opts)`, `start/pause/resume/end(id)`, `findByPin(pin)`, `activeForRoom(roomId)`, `join(pin,name)`, `progress(id,playerId,patch)`, `revive(id,playerId,toPageId)`, `reviveAll(id)`, `openHint(id,pageId)`, `notify/clearNotice(id,text)`, `setOpts(id,patch)`, `kick`, `heartbeat/setOffline`, `nodeCounts/leaderboard/stats(id)`, `seedDemo(id,n)`(시연용 가상 참여자), **`roundsForRoom(roomId)`·`latestForRoom(roomId)`·`report(sid)`**(리포트용).
- **최초 1회 시드**(`seedOnce()`, 플래그 `bangtalN.sessions.seeded.v1`): 첫 방문 때 예시 상태를 깔아 화면을 바로 확인할 수 있게 한다 — **r1 = 미션(누적 7명) + 실시간 회차2(진행 중, 7명) + 회차1(종료)** / **r2 = 미션만**. 즉 r1 카드에서 두 배지·두 PIN이 동시에 보인다. 이미 세션이 있거나 한 번 깔린 뒤에는 실행되지 않아 사용자 데이터를 덮지 않는다(`resetAll()` 후에도 재실행 안 됨).
- 세션에는 `round`(그 방에서 몇 번째 회차인지)가 있고, 방별로 **최근 20회차만 보관**한다(`KEEP_PER_ROOM`, `pruneRooms()` — 진행 중 회차는 안 지움). localStorage 용량 보호용.
- 세션 모델: `{ id, roomId, title, pin, state(lobby|running|paused|ended), opts{lateJoin,revive(host|self|both),revivePenalty,speedBonus,nickApproval}, startId, qIds[], total, players[], notice, openHints{} }`. 참여자: `{ id, name, curId, score, solved, wrong, hintUsed, revived, status(playing|failed|done), online, enteredNodeAt, trail[] }`. `stuckMs`/`isStuck`는 읽을 때 파생(3분 이상 한 노드 체류 = 정체).

## 실시간 모드 기획 (A+B: 관제탑 + 부활)
방탈출은 분기(정답/오답 출구)가 있어 참여자가 서로 다른 노드에 흩어지므로, 카훗식 락스텝(전원 같은 문제)은 성립하지 않는다. 대신:
- **A. 진행자 = 관제탑.** 문제를 진행하지 않는다. 편집 화면의 노드 좌표를 그대로 재사용한 **지도 위에 참여자 아바타**를 띄우고, ①어디에 사람이 몰려 정체 중인지 ②누가 실패해 갇혔는지를 보며 개입(힌트 전체 공개·부활·공지·일시정지)한다. 방탈출 카페 직원이 CCTV 보며 힌트 주는 역할.
- **B. 실패 엔딩 = 탈락이 아니라 "잠깐 멈춤".** 실시간에서 `wrongExit`→실패 엔딩에 도달하면 게임오버가 아니라 부활 대기 상태가 되고, 진행자가 부활시키거나(`revive:'host'`) 스스로 "다시 도전"(`'self'`)해서 틀린 문제(`lastQuestionId`)로 복귀한다. 점수만 `revivePenalty` 차감 → **실패 = 시간 손해**. 실패자가 남은 세션 내내 방치되는 문제를 해결.
- 1차는 **개인전**(팀전은 `opts.team` 자리만 남김).
- 참여자 화면은 기존 `참여자 플레이`를 그대로 쓰고 4가지만 추가: 실시간 배지+진도 미니맵 / 진행자 공지 배너 / 일시정지 잠금 오버레이 / 부활 대기 화면. 진행자가 `openHint`한 문제는 힌트 버튼 없이 자동 공개.

## 전체 화면 모음 (마스터 파일)
`전체 화면 모음.dc.html` 은 프로젝트의 모든 화면을 축소 미리보기(iframe)로 모아 보여주는 마스터 파일이다.

**규칙: 새 화면(.dc.html)을 만들 때마다 이 파일에도 카드를 추가한다.**
- 화면별로 흰색 패널 하나(아이콘 + 제목 + 설명)를 두고, 그 안에 웹/모바일 등 버전별 미리보기 카드를 나란히 배치한다.
- 각 미리보기는 `position:relative; overflow:hidden` 박스 안의 `<iframe src="파일명.dc.html" scrolling="no">` 에 `transform:scale(...)`(origin top-left)로 축소하고 `pointer-events:none`을 준다. 박스 크기 = iframe 크기 × scale.
- 각 카드 헤더 우측에 실제 파일을 여는 `열기` 링크(`<a href="파일명.dc.html">`)를 둔다.
- 상단의 화면/파일 수 카운트도 갱신한다.

## body margin 리셋 (모든 화면 공통)
각 화면 `<helmet>` 의 `<style>` 첫 줄에 **`body { margin: 0; }`** 을 둔다. `support.js` 는 `html,body{margin:0}` 리셋을 `if (!parsed.preview)` 일 때만 주입하는데(132·169줄), 이 프로젝트 화면은 대부분 `data-props` 에 `$preview` 를 갖고 있어서 그 리셋이 **건너뛰어지고 브라우저 기본 8px 여백이 남는다**(100vh 화면이 8px 밀려 스크롤이 생김). 새 화면을 만들 때 이 한 줄을 빠뜨리지 말 것. `support.js` 는 벤더 런타임이라 수정하지 않는다.

## 디자인 시스템 / 톤
- QuizN Design System, SHOW(오렌지) 테마. `_ds/quizn-design-system-019dd1f4-2c70-7609-916f-874d459447f7/` 번들 사용.
- 폰트: `var(--font-kr)` / `var(--font-small)` / `var(--font-en)`. 아이콘: fontello (`icon-*`).
- 브랜드명 표기: 방탈출N (N은 `var(--main-color)` 강조).
- 캐릭터/이미지: `uploads/character01/` (예: play_loading_bg.png 무지개 버스).

## 용어 통일 (실시간 / 미션) ★
**화면 표기는 한글 `실시간` · `미션`, 코드 식별자는 영문 `realtime` · `mission`** 으로 통일했다.
- 화면에 `LIVE` 를 쓰지 않는다. 예전 `LIVE` 배지·필터·안내문은 모두 `실시간` 으로 바꿨다 — 되살리지 말 것.
- 코드에서 `live` 를 쓰지 않는다: `kind:'realtime'`, `roomState().kind === 'realtime'`, `roomState().realtime`(예전 `.live`), CSS 토큰 `--mode-realtime-*`(예전 `--mode-live-*`), 딥링크 `?realtime=1`(예전 `?live=1`), 필터 키·카운트 키 `realtime`.
- **`kind` 는 localStorage 에 `'mission'` 만 저장된다**(실시간 세션은 `kind` 를 안 쓰고 `undefined`). 그래서 `live`→`realtime` 개명에 데이터 마이그레이션이 필요 없었다. 판정은 항상 `s.kind === 'mission'` / `!== 'mission'` 으로 하고, `=== 'realtime'` 으로 저장값을 비교하지 말 것.
- ⚠️ 개명 시 **필터 탭 키와 카운트 객체 키가 같이 바뀌어야 한다**(`counts[t.k]` 로 조회하므로 한쪽만 바꾸면 카운트가 `undefined` 가 되고 필터가 빈 목록이 된다). 실제로 이 실수가 났다.
- `미션` 은 "문제 한 개"라는 뜻으로도 읽힐 수 있으니, 퍼즐을 가리킬 때는 **`문제`** 를 쓴다.

## 회차/미션 메모 (session.memo) ★
메모는 **세션 단위**다(미션·실시간 회차 둘 다). 한 방에 미션·회차가 여러 개 생기므로 "이게 어떤 건지" 구분하는 게 목적이다 — 미션은 `3반 과제`, 회차는 `3반 6교시` 처럼. `room.memo` 를 만들었다가 **제거했다**(방에 하나만 달면 개별 구분이 안 된다).
- **입력 경로가 모드마다 다르다.** 미션은 `My 방탈출` **미션 생성 모달**(기간 아래 `메모 (선택)`)에서 미리 적을 수 있고 `openMission(roomId,{dueAt,memo})` 로 저장된다. 실시간 회차는 대기실에서 바로 만들어져 입력 단계가 없으므로 **리포트 목록에서 사후 입력**한다.
- **수정: 리포트 회차 목록의 행에서 메모 줄을 눌러 바로 고친다**(`SessionStore.setMemo(sid, memo)`, 최대 60자). 비어 있으면 `메모 추가` 로 보이고, hover 시 테두리+연필이 떠서 누를 수 있음을 알린다. 내용을 비워 저장하면 다시 `메모 추가` 로 돌아간다.
- 모달은 **모드를 따라간다**: 제목 `미션 수정`(미션) / `메모`(실시간), 부제 `미션 2회차` / `3회차`, 헤더 아이콘 색 보라/민트, 안내문 "이 미션/회차를 구분하는 데".
- **미션은 기간(마감일)도 여기서 고친다**(`SessionStore.setDueAt(sid, ts)`). 날짜 입력 + 1주/2주/1개월 칩 + `N일 뒤에 닫혀요` 안내. 마감을 미래로 늘리면 만료됐던 미션이 다시 열리고, 과거 날짜를 넣으면 경고(빨강)와 함께 저장 시 즉시 닫힌다. 실시간 회차엔 기간이 없어 메모만 뜬다.
- 노출: 리포트 회차 목록 행(기간·PIN 줄 아래 작은 글씨) + **리포트 상세** 상단. `report()` 가 `memo` 를 함께 돌려준다.
  - ⚠️ 행 전체가 `<a>` 링크이므로 메모 버튼 핸들러에서 **`preventDefault()` + `stopPropagation()`** 을 반드시 호출한다. 안 하면 리포트 상세로 새 버린다.
  - ⚠️ 어느 세션인지 찾을 때 **`sid` 로 정확히 비교**한다(`href.indexOf(id)` 같은 부분문자열 매칭은 id 접두어가 겹치면 엉뚱한 행을 잡는다).
- **아이콘: 메모 = `icon-edit-02`**(페이지에 쓰는 연필), 행의 수정 표시 = `icon-edit`. 이 fontello 셋에는 **쪽지/노트 아이콘이 없다** — `icon-newdoc` 은 내보내기 화살표, `icon-note-beamed` 은 음표, `icon-text` 는 텍스트 커서다(`icon-pencil`·`icon-note`·`icon-doc-text` 는 아예 없음). 행 메모에는 아이콘을 붙이지 않는다(시끄러워서 뺐다).

## My 방탈출 카드 액션 구조 ★
카드 하단 주 버튼은 **`실시간 시작`(민트) / `미션 생성`(보라)** 두 개다. 카드의 주 행동은 "이 방을 어떻게 진행할지"이기 때문이다.
- **⋮ 더보기 메뉴는 `편집하기 · 미리보기 · 복사하기 · 결과 리포트 · 공유하기 · 삭제하기`** 순서다. 편집하기가 맨 위. 여닫는 항목(미션·실시간)은 **메뉴에 두지 않는다** — 하단 버튼과 중복이라 뺐다.
- 버튼 라벨에 건수를 넣지 않고 **항상 행동으로 읽히게** 둔다(`미션 2건` 처럼 쓰면 상태 표시로 읽힌다). 건수는 썸네일 배지와 `PIN 보기` 팝업이 알려준다.
- 용어: **`미션 생성`**(`미션 열기` 아님) · **`복사하기`**(`복제하기` 아님).

## 모드/상태 색 토큰 (mode-colors.css)
**모드 색과 상태 색은 서로 다른 축이다.** 둘을 같은 색으로 쓰면 "실시간인지"와 "진행 중인지"를 구분할 수 없다. 색은 `mode-colors.css` 한 곳에만 두고 화면은 `var(--…)` 로만 참조한다 — **하드코딩 금지**.
- 각 화면 `<helmet>` 의 `colors_and_type.css` **다음 줄**에 `<link rel="stylesheet" href="mode-colors.css">`(모바일은 `../mode-colors.css`)를 로드한다. 새 화면 만들 때 빠뜨리지 말 것.
- **모드 = 퀴즈앤 대표 컬러.** 실시간 = **민트**(`--mode-realtime` `#00C1B0`, brand-board-sub), 미션 = **보라**(`--mode-mission` `#9359E1`, brand-video). 각각 `-deep`/`-soft`(테두리)/`-bg`(연한 배경) 변형이 있다. 실시간 배지·미션 칩·모드 안내 박스·PIN 팝업 모드칩·⋮메뉴의 미션/실시간 항목·미션 열기 모달이 모두 이 색.
- **상태 = 진행전/진행중/완료 → 검정/초록/빨강.** `--state-pre`(`#191919` 부드러운 검정)/`--state-ing`(`#0a7d57`)/`--state-done`(`#b3352b`), 각각 `-bg`/`-soft` 변형. 완주 인원·평균 완주율 같은 "성공" 표시도 `--state-ing` 을 쓴다.
- **⚠️ 상태 색은 모드에 따라 갈리지 않는다.** 미션 행이든 실시간 행이든 `진행 중` 은 똑같이 초록이다. 모드는 모드칩 하나로만 전달하고, 상태 칩에 `--mode-*` 를 넣지 말 것(실제로 미션 행 상태칩을 보라로 만들었다가 지적받아 고쳤다). 리포트 회차 목록의 `STATE` 맵을 두 행이 공유하게 두면 이 실수가 안 난다.
- **⚠️ 모드 아이콘 타일은 상태에 따라 색을 바꾸지 않는다.** 리포트 회차 목록의 좌측 회차번호 타일(`badgeStyle`)은 종료된 회차만 오렌지(`--bg-light-color`)로 튀던 버그가 있었다. 타일은 "이게 실시간/미션이다"라는 모드 표시이고 진행 상태는 옆 상태칩이 말하므로, **열림/종료와 무관하게 모드색 고정**이다.
- **예외로 토큰을 쓰지 않는 색**(의미가 다르므로 그대로 둔다): ①`실시간 관제` 지도의 노드/연결선 — 성공 엔딩=초록 `#12b57e` / 실패 엔딩=빨강 `#e0483d` 규칙(위 관제 절 참고), ②정체 경고 스타일, ③아바타 랜덤 팔레트(`AV_COLORS`), ④삭제/로그아웃 등 일반 danger hover(`#fdeaea`), ⑤리포트의 문제 난이도 진단색(어려움 `#c0392b` 등).

## 파일 구성
- 통합본(웹+모바일 나란히, 캔버스): `로그인 화면` · `회원가입 화면` · `비밀번호 찾기 화면` · `랜딩 페이지`
- 분리본 웹(단일 화면, 전체화면, 프로젝트 루트): `로그인 - 웹`, `회원가입 - 웹`, `비밀번호 찾기 - 웹`, `랜딩 - 웹`
- 분리본 모바일: `모바일/` 폴더에 모음 — `모바일/로그인 - 모바일`, `모바일/회원가입 - 모바일`, `모바일/비밀번호 찾기 - 모바일`, `모바일/랜딩 - 모바일`. 하위 폴더라서 자산 참조는 `../support.js`, `../_ds/...`, `../uploads/...` 로 쓴다. 모바일끼리의 링크는 같은 폴더라 파일명만 쓴다.
- `방탈출 편집 화면` (PC 전용 단일 화면)
- `문제 대화 편집` (PC 전용). 페이지 내용(배경·대화·문제)을 참여자 채팅 형식으로 통합 저작하는 화면. 상단 무대 배경 설정 행(변경=프리셋 순환) + 좌측 말풍선 블록 에디터(드래그 순서변경·발화자 select[호스트/내레이터/캐릭터]·타입[텍스트/이미지/딜레이]·⋯복제/삭제·빈상태) + 문제(자물쇠) 블록(페이지당 1개·항상 마지막, 추가버튼은 이미 있으면 "문제 1개" 비활성, 자물쇠 5종 grid[객관식mc/주관식short/OX/순서order/핫스팟hotspot]·타입별 정답 입력·정답/오답 출구 요약), 우측 폰 프레임 실시간 프리뷰(배경 위 아바타+말풍선, ▶재생=순차 등장 애니메이션·딜레이 반영 후 문제 위젯 등장, 정답 표시 토글). `방탈출 편집 화면`으로 뒤로가기/저장.
- 참여자 입장(PIN 접속): `참여자 입장 - 웹`, `모바일/참여자 입장 - 모바일`. 2단계(PIN 7자리 셀 타이핑→닉네임), `입장하기`는 `참여자 플레이 …` 파일로 링크, `?step=name`으로 닉네임 단계부터 렌더 가능.
- 참여자 플레이(학생 문제풀이): `참여자 플레이 - 웹`, `모바일/참여자 플레이 - 모바일`. 게임형 풀스크린(상단 점수+진행바). 상황 설명을 말풍선으로 한 문장씩 순차 공개 후 문제 위젯 등장. 자물쇠 5종(객관식·OX·주관식·순서·핫스팟), 힌트 3정책(free/count/deduct), 점수 1000 시작·힌트로만 차감, 성공/실패 엔딩(점수·소요시간·다시하기/처음으로). 배경은 지금 밝은 톤(편집화면에서 지정 예정). `?p=<pageId>`로 특정 페이지부터 렌더 가능(자물쇠: 객관식 `?p=p1`·OX `?p=p2`·주관식 `?p=p3`·순서 `?p=p4`·핫스팟 `?p=p5`, 성공 엔딩 `?p=p7`, 실패 엔딩 `?p=p8`; `?p=p1&hint=ask`는 힌트 차감 확인 모달 — 모두 모음집 미리보기용, 딥링크 시 나레이션 자동 전개). 전체 화면 모음의 플레이 섹션은 웹/모바일 각각 자물쇠 5종·힌트·성공/실패 엔딩을 자물쇠별 소제목(웹+모바일 페어)으로 그룹화해 노출. 입장 섹션도 1단계 PIN / 2단계 닉네임 소제목으로 그룹화. 모음집 섹션 순서: …My 방탈출 → 결과 리포트 → 마이페이지.
- My 방탈출 목록(교사용, 웹 와이드): `My 방탈출 - 웹`. 카드 그리드(썸네일 배너·제목+⋮더보기·상태배지·문제수·PIN·공개여부·작성일), 액션(하단 `실시간 시작`·`미션 생성`, ⋮ 메뉴: 편집하기·미리보기·복사하기·리포트·공유·삭제), 상단 검색/상태필터/정렬/새 방탈출, 빈 상태(캐릭터+만들기 유도).
- 마이페이지(교사용, 웹 와이드): `마이페이지 - 웹`. 상단 프로필 드롭다운(마이페이지·로그아웃 — 계정설정은 중복 제거) + 좌측 탭 메뉴/우측 내용 설정형(프로필 카드 없이 탭만). 섹션: 프로필·계정 설정·알림 설정·테마/표시·구독/요금제. 상단 알림은 `icon-megaphone`+빨간 배지.
- 결과 리포트(교사용, 웹 와이드) **3화면 · 회차(세션) 단위**:
  - `리포트 회차 목록 - 웹`(`?id=방`) — 그 방의 기록 목록. 요약3(실시간 회차/누적 참여자/평균 완주율) + **모드 필터 탭(`전체 / 미션 / 실시간`)** + 행 목록.
    **행 구조(중요):** 좌측 아이콘 타일은 **없다**(제거됨). 제목줄이 **`모드+번호 칩` → `메모` → `상태칩`** 순이고, 그 아래 회색 한 줄에 **날짜 · 소요시간 · PIN** 이 온다.
    - 칩 표기는 **`실시간 N회차` / `미션 N회차`** 로 통일한다(미션이 하나뿐이면 그냥 `미션`). 모드색(민트/보라)을 칩에 입혀 이 칩 하나가 모드·번호를 다 말한다.
    - **메모가 이 기록의 "이름"** 이다. 있으면 14px 진한 글씨로 제목처럼, 없으면 점선 `메모 추가` 로 흐리게 떠서 이름 붙이기를 유도한다. 눌러서 바로 수정.
    - **날짜를 제목으로 쓰지 말 것.** 예전엔 날짜가 15.5px 볼드 제목이었는데, `19:39` vs `19:31` 처럼 구별이 안 되고 목록이 이미 날짜순 정렬이라 순서가 곧 날짜다. 사용자는 "3반 6교시" 같은 이름으로 기록을 찾는다.
    - **미션 행에는 소요시간을 두지 않는다**(`기간 중 누적` 같은 문구를 넣었다가 뺐다 — 위 줄의 기간이 이미 말하고, 미션엔 '소요시간'이라 부를 값이 없다). 실시간 회차만 `22분 00초` 를 보여준다.
    - 요약 카드와 탭 카운트는 **필터와 무관하게 전체 기준**이다. 필터 결과가 0건일 때는 "기록이 아예 없음"과 다른 안내를 띄운다(`filteredEmpty`).
  - **⚠️ 미션 행은 `missionsForRoom(roomId)` 로 전부 그린다.** `missionFor()` 는 가장 최근 미션 하나만 주므로, 미션을 여러 개 연 방에서 **나머지 미션 기록이 목록에서 사라진다**(실제로 그랬다). 미션이 2개 이상이면 좌측 타일에 `미션 1`·`미션 2`(`seq`)로 구분한다.
  - 설명 문구는 "미션은 누적 기록 **하나**" 라고 쓰지 말 것 — 한 방에 미션이 여러 개일 수 있어 사실과 다르다. 현재 문구: "미션은 열어둔 기간 동안 참여한 사람이 계속 쌓이고, 실시간은 회차를 열 때마다 기록이 새로 생깁니다."
  - `리포트 상세 - 웹`(**`?sid=회차`**) — 요약5카드+탭3(학생별표·문제별진단·참여현황). 제목 옆 **모드 칩에 회차까지 넣는다** — `실시간 3회차` / `미션 2회차`(미션이 하나뿐이면 그냥 `미션`). 리포트 회차 목록의 행 라벨과 **정확히 같은 표기**라 어느 기록을 열었는지 바로 확인된다. 실시간이면 그 옆 **`회차 바꾸기`** 버튼으로 다른 회차로 전환한다(예전엔 이 버튼이 `3회차` 라서 칩과 중복됐다). `report()` 가 `round`·`seq`·`dueAt` 을 함께 돌려준다.
    - 칩 옆에는 **PIN 이 아니라 시간**을 둔다: 미션은 **진행 기간**(`2026.07.24 21:17 ~ 08.01 21:17`, 끝이 같은 해면 연도 생략), 실시간은 **시작 날짜**. 끝난 회차 PIN 은 만료돼서 쓸모가 없으므로 상세에서 뺐다(진행 중 PIN 은 `My 방탈출` 의 `PIN 보기` 팝업에서 본다). 학생별표의 **`탈출 시간`** 컬럼은 성공 엔딩까지 걸린 시간(`분 초`)이며 완주자만 값이 있다(미완주 `—`). `?id=방`만 온 예전 링크는 그 방의 **최근 회차**로 자동 폴백하고, 세션이 아예 없으면 데모 시나리오(`SCEN`)로 폴백한다(모음집 미리보기용).
  - `리포트 개인별 상세 - 웹`(`?sid=&name=`) — 문제별 풀이 타임라인. 돌아가기 링크가 `sid`를 유지해 같은 회차로 복귀.
  - **데이터는 더미가 아니라 세션 실기록**이다. `SessionStore.report(sid)` 가 `players[].trail`(문제별 통과 시각)을 집계해 `perQuestion`(시도/통과/정답률/평균소요)과 `players[].perQ` 를 만든다. 상세 화면의 `fromSession()` 이 이걸 기존 셀 표기(`c`정답/`w`오답/`a`풀이중/`n`미도달)로 변환한다 — **셀 코드에 `.` 같은 새 값을 쓰면 `ord`/`map` 조회가 깨지니 이 4개만 쓸 것.**
  - **기간 선택 UI 는 제거했다**(날짜 2개 + `전체`/`최근 7일`/`최근 30일` 칩이 있었다 — 화면이 복잡해져서 뺐다). 범위 조회는 **`?from=YYYY-MM-DD&to=YYYY-MM-DD` 딥링크로만** 남아 있고, `rangeOpts()` 가 그걸 읽어 `report(sid,{from,to})` 에 넘긴다. 다시 UI 를 붙일 일이 있으면 이 함수를 재사용할 것.
  - **메모는 제목 바로 아래 작은 회색 글씨 한 줄**이다(테두리 박스·아이콘·`메모` 라벨 없음 — 다 뺐다).
  - **개인 소요시간 기준점이 모드마다 다르다:** 실시간은 `session.startedAt`(모두 같은 출발선), **미션은 `player.joinedAt`**(아무 때나 들어오므로 세션 시작을 쓰면 며칠짜리 값이 나온다). `report()` 의 `base` 계산 참고 — 여기를 통일하면 미션 탈출 시간이 망가진다.
  - 진입: `실시간 결과` 화면의 "결과 리포트" 버튼은 방금 끝난 그 회차(`?sid=`)로 직행한다.
- 실시간 모드 화면(A+B 기획은 위 "실시간 모드 기획" 절 참고):
  - `실시간 대기실 - 웹` (PC 진행자). 다크 테마. 좌측 PIN 대형 표시(6칸)+세션 설정 스위치(시작 후 입장/속도 보너스/닉네임 승인)+시작 버튼, 우측 입장자 칩 그리드(빈 상태=캐릭터 버스). `?id=`로 방 지정(활성 세션 없으면 자동 생성), `?sid=`로 기존 세션. 시작하면 관제 화면으로 이동.
  - `실시간 관제 - 웹` (PC 진행자, **핵심 화면**). **어드민이 아니라 보드게임 판** 톤 — 밝은 크림/오렌지 배경 + 점무늬, 둥근 방(노드) 카드, 이모지 섞은 친근한 말투("막힌 친구들"·"다시 보내주기"·"모두에게 한마디"·"잠깐 멈춰"·"마치기"). 헤더(실시간·PIN·경과시간) + 통계 pill 5개(친구들/풀고 있어요/막혔어요/쉬고 있어요/탈출했어요) + 전체 진행 바 + 좌측 지도 + 우측 패널 탭3(**순위**/친구들/도와주기 — 순위가 기본 탭).
    - **방 = 둥근 노드 마커.** 네모 카드가 아니라 원형이다: `groundStyle`(아주 옅은 그림자) + `discStyle`(평평한 단색 원 + 2.5px 테두리) + `innerRingStyle`(안쪽 얇은 실선 링) + 이모지 아이콘, 좌상단 문제번호 뱃지, 우하단 인원 뱃지, 그 아래 이름표(`plateStyle`)에 제목·자물쇠·경고 칩. 사람이 있으면 지름이 66→74px 로 커지고, hover 시 살짝 튄다.
    - **색 규칙: 성공 엔딩=초록 / 실패 엔딩=빨강 / 나머지(출발·문제·이야기)=노랑(앰버).** 아이콘으로 종류를 구분한다(🚩 출발 · 🔒 문제 · 💬 이야기 · 🏁 성공 · 💥 실패). 정체 중인 노드는 테두리만 빨강(`#e0483d`)으로 덮고 은은한 `lvGlowPulse` 링이 돈다.
    - **톤: 백지 + 톤다운.** 맵 박스는 흰 배경 + 연한 점 모눈(22px)뿐이고, 테두리는 `var(--border-2)` 1px. 노드는 진한 그라데이션 대신 **연한 배경(`fill`) + 진한 테두리(`line`) + 진한 글씨(`ink`)** 조합이라 색이 상태 전달에만 쓰인다. 화면 배경도 `#f5f5f7` 중성 회색이고, 오렌지는 브랜드 액션(버튼·인원 뱃지·활성 탭)에만 남긴다. 이전의 양피지/사탕색 조합은 촌스럽다는 피드백으로 폐기했다 — 다시 warm 계열(`#ffe0c8`·`#b4661f` 등)을 끌어오지 말 것.
    - **배치 = 가로 순차 다이어그램** (`layoutNodes()`). 편집 화면의 x/y 좌표는 **무시하고 자동 정렬**한다: 시작 노드에서 `correctExit` 를 따라가며 메인 라인을 왼→오른쪽 등간격으로 놓고(0번 줄), 오답으로 갈라지는 노드(주로 실패 엔딩)는 부모 다음 열의 아래 줄에 놓는다. 편집에서 노드를 아무렇게 흩어놔도 관제는 항상 깔끔한 다이어그램으로 보인다.
    - 연결선은 **직각 다이어그램**: 같은 줄이면 수평 직선, 줄이 다르면 **세로로 먼저 빠진 뒤 수평 이동**하는 ㄴ자 꺾임(`Q` 로 둥글게). 수평을 먼저 가면 중간 노드를 관통하므로 순서가 중요하다. 정답=초록 실선+화살표, 오답=빨강 점선+화살표(`marker-end`, `<defs>` 의 `lv-arrow-ok/no`). 선은 원 테두리에서 `PAD` 만큼 띄워 시작/끝난다.
    - `fit()` 은 다이어그램을 뷰포트 **정중앙**에 놓는다(`(vw-cw*z)/2 - minX*z`). 여기에 상수 offset 을 더하면 중앙 정렬이 깨지므로 주의.
    - **참여자 = 캐릭터 프로필 뱃지.** `uploads/character01/profile_{Cula,Frankie,Kay,Kebi,Kongkong,Zizi}.png` 6종을 닉네임 해시(`faceOf`)로 안정 배정. 뱃지는 타일 **위쪽에 나란히 서고**(4명/줄, 살짝 겹쳐 옹기종기) 노드 사이를 `transition:left/top .85s`로 **부드럽게 이동**한다. 상태별로 링 색·모션·이모지가 다름: 진행중=`lvIdle`(통통), 막힘=`lvStuck`(좌우로 흔들림)+😣, 실패=정지+💤, 탈출=`lvIdle`+🎉. 노드 이동 시 `lvLand`+반짝 링(`_landedAt` 800ms). hover 시 이름표.
    - 캐릭터가 마커 위에 서므로 `layoutNodes()`의 `COL/ROW` 간격·`fit()` 상단 여유(`HEAD`)·연결선의 `DISC`(마커 지름 근사값)가 모두 연동된다 — `NODE_W/NODE_H/CH` 나 마커 지름을 바꾸면 이 값들도 같이 조정할 것.
    - **`?demo=1`**: 예시 참여자를 자동 배치(막힘·실패·탈출 상태 섞어서)하고 2.6초마다 2~3명씩 정답 출구로 옮겨 **뱃지 이동을 눈으로 보여준다**. 엔딩에 닿으면 출발점으로 되돌려 계속 순환. 하단 `▶ 예시 친구 이동 보기` 버튼으로도 켜고 끌 수 있다(`startDemo`/`demoStep`).
    - 도와주기 탭 = 모두에게 한마디(빠른 문구 칩)·막힌 친구들(막힌 친구 얼굴 표시, 힌트 열어주기/공지)·다시 보내주기(개별/전원 부활).
  - `실시간 참여자 대기 - 웹` / `모바일/실시간 참여자 대기 - 모바일`. 내 닉네임 카드+함께 기다리는 친구 칩+팁 로테이션, 세션 state에 따라 대기→"시작됐어요!"(입장 버튼)→"종료됐어요"로 자동 전환.
  - `실시간 결과 - 웹` (PC 진행자). 다크. 요약4카드+시상대 TOP3(2·1·3위 배치)+전체 순위 표(상태·해결·힌트·부활·점수)+문제별 통과율 막대 → 결과 리포트로 연결.
  - 진입: `My 방탈출` 카드 ⋮더보기 **"실시간으로 시작"**(활성 세션이 있으면 "실시간 관제 열기"), 진행 중인 방 썸네일에 실시간 배지. 참여자는 `참여자 입장`에서 PIN 입력 시 실시간 세션이면 실시간 안내 후 대기실로, 아니면 기존처럼 바로 플레이로 분기.
  - 모음집 미리보기용 딥링크: `참여자 플레이 …?live=1`(세션 없이 실시간 UI), `&notice=문구`(공지 배너), `&paused=1`(일시정지 잠금), `?p=p8&live=1`(부활 대기).
- **PIN은 6자리**(`NNN NNN`). 입장 화면이 7자리를 요구해 실제 PIN이 통과되지 않던 버그를 6자리로 수정함 — PIN 관련 UI/검증 추가 시 6자리 기준.
## 열림 상태는 세션에서 계산한다 (room.status 제거됨) ★
예전에는 `room.status(pre|ing|done)` 를 **사람이 편집 화면에서 수동으로** 바꿨다. 실시간·미션이 생기면서 실제 상태와 어긋나는 문제가 있어(진행 전인데 회차가 열려 있는 등) **필드를 완전히 삭제했다.**
- 유일한 판단 기준은 **`SessionStore.roomState(roomId)`** → `{ kind:'realtime'|'mission'|'closed', realtime, mission, pin }`. 둘 다 열려 있으면 `live` 우선.
- 표기: 🔴 **실시간 N건 진행 중**(점 깜빡임) / 🟢 **미션 N건 진행 중**. 썸네일 **좌상단에만** 둔다(하단 중복 배지는 제거됨).
- **썸네일에 `닫힘` 칩은 두지 않는다.** 닫힌 방은 아무 배지도 없는 것으로 충분하고(빈 상태가 곧 닫힘), 대신 본문에 `진행중인 모드가 없습니다.` 안내 줄이 있다. `닫힘` 이라는 말은 **상단 필터 탭에만** 남는다.
- **공개/비공개는 썸네일이 아니라 생성일 옆 아이콘**으로 표시한다(🌐/🔒). 썸네일 우상단 배지는 제거됨 — 상태 배지와 경쟁해서 시끄러웠다.
- **실시간과 미션은 동시에 열릴 수 있다.** 그래서 `mapRoom` 은 단일 `open` 값이 아니라 **독립 플래그** `openRealtime`/`openMission`/`openClosed` 를 담고, 배지도 둘 다 노출한다.
- `My 방탈출` 상단 필터는 `전체 / 실시간 / 미션 / 닫힘`(옛 `진행 전·진행 중·종료` 아님). **겹치는 방은 실시간·미션 카운트에 모두 포함**된다 — 배지가 둘 다 뜨는 것과 일관되게 맞춘 것이니 한쪽만 세도록 되돌리지 말 것.
- **편집 화면의 수동 상태 드롭다운은 제거**했다. 여닫는 건 `My 방탈출` 카드 하단 버튼에서만 한다(`미션 생성` / `실시간 시작`). 편집 화면 배지는 읽기 전용이고, PIN 칩도 **열려 있을 때만** 그 세션 PIN 을 보여준다.
- `session-store` 는 더 이상 `RoomStore.saveMeta({status})` 를 호출하지 않는다. **다시 status 를 되살리지 말 것** — 두 곳에서 상태를 관리하면 반드시 어긋난다.

## 두 가지 진행 방식 (미션모드 / 실시간모드) ★
한 방을 **두 방식으로 다 쓸 수 있다.** 방에 `mode` 필드는 없고, "어떤 세션을 열었는지"로만 갈린다. 세션 모델의 **`kind`** 가 구분자다.

| | **미션모드** (`kind:'mission'`) | **실시간모드** (`kind` 없음/`'live'`) |
|---|---|---|
| 진행자 | 없음 | 필수 (관제 화면) |
| 입장 | 열려 있는 동안 **아무 때나** | 진행자가 회차를 연 동안만 |
| PIN | **방 고정 PIN**(`room.pin`) 상시 | **회차마다 새로 발급**, 종료 시 만료 |
| 회차 | 없음 (기록 계속 누적) · 여러 개면 `seq` 로 구분 | 1·2·3회차… |
| 입장 후 | **바로 플레이** | 대기실 → 진행자가 시작 |
| 여는 법 | My 방탈출 카드 하단 **`미션 생성`** | 카드 하단 `실시간 시작` |
| 리포트 | 누적 1개 · **제작자가 기간 지정**해서 봄 | 회차별 |

- **한 방에 미션·실시간을 여러 개 동시에 돌릴 수 있다**(1반·2반 동시 수업, 과제 A·B 등). `openMission(roomId, {dueAt})` 은 호출할 때마다 새로 만들고 **각자 PIN 을 발급**한다(`seq` 로 구분).
- **미션은 기간제**다. `dueAt`(종료 예정일, 기본 2주 뒤)을 갖고, 마감이 지나면 `isOpen()` 이 false 가 되어 **PIN 이 자동 만료**된다(입장 차단·목록 제외). `My 방탈출` 카드 하단 `미션 생성` → **기간+메모 모달**(날짜 입력 + 1주/2주/1개월 칩 + 메모 한 줄)에서 확인해야 실제로 열린다. **`isOpen(s)` 이 열림 판단의 단일 기준**이다 — `state !== 'ended'` 만 보면 마감 지난 미션이 열려 있는 것으로 잡히니 반드시 이걸 쓸 것. 실시간도 `create()` 가 중복을 막지 않는다 — 대기실에 **`&new=1`** 을 붙이면 진행 중 회차가 있어도 새 회차를 만든다.
- API: `openMission(roomId,{dueAt,memo})` / `closeMission(sessionId|roomId)` / `missionFor(roomId)`(가장 최근 미션 하나) / **`missionsForRoom(roomId)`**(그 방 미션 전체·최근순 — 리포트 목록용) / **`openSessionsFor(roomId)`**(지금 열려 있는 미션+실시간 전체, 실시간 먼저·최근순 — 팝업용).
- **`kind !== 'mission'` 필터 주의:** `activeForRoom`·`roundsForRoom`·`nextRound`·`pruneRooms` 는 모두 미션을 제외해야 한다. 미션은 항상 `running` 이라서, 안 걸러내면 관제 화면이 미션 세션을 회차로 오인한다.
- **방 `status` 동기화 규칙:** 실시간 회차를 끝내도 **미션이 열려 있으면 `ing` 유지**, 반대도 마찬가지. 둘 다 닫혔을 때만 `done`. (한쪽만 보고 `done` 으로 덮으면 배지가 실제와 어긋난다)
- `report(sid, {from, to})` — 타임스탬프 범위를 주면 그 기간에 참여한 사람만 집계한다(미션 리포트용). 반환값에 `kind`·`allPlayerN`(기간 필터 전 인원) 포함.
- **PIN 소유권 (실시간 = 카훗 방식).** 참여자는 진행자가 회차를 열 때 발급되는 **세션 PIN**으로만 입장한다.
  - `session-store.js` `freshPin()` 이 세션 생성 시 6자리를 새로 뽑고(살아있는 다른 세션과 중복 회피), `end()` 시 그 PIN 은 만료된다 → 지난 회차 PIN 으로는 아무도 못 들어옴.
  - `create(roomId)` 는 **같은 방에 살아있는 세션이 있으면 새로 만들지 않고 그것을 반환**한다(세션 중복 방지). 중복되면 `activeForRoom()` 이 마지막 것만 돌려줘 앞 세션 참여자가 유령이 된다.
  - `start()`/`end()` 가 `RoomStore.saveMeta` 로 방 `status` 를 `ing`/`done` 으로 자동 동기화한다 — 목록 배지와 세션 상태가 어긋나지 않게.
  - `참여자 입장` 은 **세션 PIN만** 검증한다(`findByPin`). 방 PIN·없는 PIN·종료된 PIN 은 다음 단계로 넘어가지 못하고, 실패 사유(종료됨/지각 차단/무효)를 그대로 보여준다. **`room.pin` 으로 입장 검증하면 종료된 회차로도 입장되는 사고가 재발하니 절대 쓰지 말 것.**
  - **PIN 은 카드에 직접 나열하지 않는다.** 세션이 여러 개일 수 있어 카드가 터지므로, PIN 줄은 **`진행 중 N건 · PIN 보기`** 버튼 하나이고 누르면 **팝업**이 열린다. 팝업의 각 행 = 모드 칩(🟢미션/🔴실시간) · 이름(`미션 2` / `3회차`) · 참여 인원 · **시간 표기** · PIN+복사 · 이동 버튼(실시간→관제, 미션→리포트). 시간 표기는 모드마다 다르다: **실시간=경과 시간**("14분 전 시작" — 지금 막 시작했는지가 중요), **미션=기간+남은 일수**("07.24 14:11 ~ 08.01 14:11" + `D-2` 칩, 3일 이내면 빨강). 복사는 `copyPin(sessionId, pin, label)` 로 행마다 분리.
  - `room.pin` 은 이제 **미션 첫 회차의 시드 PIN** 정도로만 남는다(새로 여는 미션은 `freshPin()`).
  - `참여자 입장` 은 `findByPin` 으로 두 종류를 모두 받고, `kind` 로 갈라 보낸다: 미션→**바로 플레이**, 실시간→대기실. 열려 있지 않은 PIN 은 다음 단계로 못 넘어간다.
- 아이콘 주의: 이 fontello 셋에 `icon-bell`·`icon-dots-three-vertical`·`icon-calendar` 등은 없음(빈 글리프). 알림=`icon-megaphone`, 세로 더보기=CSS 점3개로 처리. `icon-pause`/`icon-minus`/`icon-lock`/`icon-clock`/`icon-wifi`/`icon-ok-circled`도 **없음** → `icon-pause-1`·`icon-minus-circle`·`icon-lock-filled`·`icon-time`·`icon-link`·`icon-check-1` 사용. 없는 아이콘 쓰기 전 `_ds/.../fontello-codes.css` 확인.
- 페이지 간 링크는 같은 계열끼리 연결(통합본↔통합본, 웹↔웹, 모바일↔모바일).
